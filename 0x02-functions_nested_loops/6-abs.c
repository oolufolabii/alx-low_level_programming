#include "main.h"
/**
 * _abs - checks for absolute value of an integer
 * @n: the number to check
 * Return: 1 if n > 1, 0 if n < 0
 */

int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);

}
