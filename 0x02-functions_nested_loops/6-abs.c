#include "main.h"
/**
 * _abs - checks for absolute ints
 * @int: the number to check
 * Return: 1 if n > 1, 0 if n < 0
 */

int _abs(int)
{
if (int < 0)
{
int * -1;
}
else if (n > 0)

{
_putchar(int)

return (int);

}

return (0);

}
